# ELMA admin-portal — Netlify config
#
# Build settings (base = apps/admin-portal, command, publish, headers) are
# configured in the Netlify UI for the "admin-elma" site.
#
# Secrets scanning: the Firebase *web* API key in src/firebase/config.ts is a
# public client identifier, not a secret (access is protected by Firebase
# Security Rules / App Check — see https://firebase.google.com/docs/projects/api-keys).
# It is safelisted for Netlify's smart-detection scanner via the site env var
#     SECRETS_SCAN_SMART_DETECTION_OMIT_VALUES
# which MUST be set in the Netlify UI/CLI/API — these scanning controls are NOT
# read from this netlify.toml. Do not re-add them here; it has no effect.
